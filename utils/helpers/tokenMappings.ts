const baseRoute = '/logos/symbols';

const tokenMappings = {
  moonbeam: { route: baseRoute + '/moonbeam-black.png', symbol: 'MOON' },
  ether: { route: baseRoute + '/eth.png', symbol: 'ETH' },
  bitcoin: { route: baseRoute + '/bitcoin.png', symbol: 'BTC' },
  matic: { route: baseRoute + '/matic.png', symbol: 'MATIC' },
};

export default tokenMappings;
