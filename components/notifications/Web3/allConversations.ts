import xmtp from './createClient'
// Create the client with an `ethers.Signer` from your application
export default async function allConversations(wallet:any) {
    const messages = await conversation.messages()
    return messages
}
